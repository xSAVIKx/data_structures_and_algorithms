import java.util.Arrays;
import java.util.List;

public class LargestDigest {
    public static long[] greedyAlgorithm(long[] numbers){
        if (numbers==null){
            return null;
        }
        if (numbers.length==0 || numbers.length==1){
            return numbers;
        }

        long[] sortedNumbers = new long[numbers.length];
        return null;
    }
}
